# Database tests (Foundation 001 §11)

Startup assertions, migration ledger behaviour, `db:verify` structure checks: C3–C4. Run by `test:db` as the roles they claim to be, never as a superuser (§10 step 5).
