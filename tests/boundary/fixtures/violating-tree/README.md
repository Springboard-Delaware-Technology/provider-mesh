Deliberately violating tree for A03. Every file here breaks one Foundation 001 §4.3 rule; the
boundary test runs the real configuration against this directory and asserts each rule fires.
Excluded from tsconfig, ESLint, Prettier, and the production boundary check.
