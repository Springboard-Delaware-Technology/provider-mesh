root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true
indent_style = space
indent_size = 2

# Approved documents may carry intentional Markdown hard-break trailing spaces (replit.md §10).
[*.md]
trim_trailing_whitespace = false
