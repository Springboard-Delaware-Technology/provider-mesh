# Events (Foundation 001 §4.2, §5.7)

Envelope schemas, the Domain Model §21 event-name registry, and the transactional outbox arrive
with capability C7. Modules may import this directory (boundary rule 1).
